<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>netstandard2.1</TargetFramework>
    <Nullable>enable</Nullable>
    <GeneratePackageOnBuild>True</GeneratePackageOnBuild>
    <Title>APIVerve.API.RandomFacts</Title>
    <Version>1.0.0</Version>
    <Authors>APIVerve</Authors>
    <Company>APIVerve</Company>
    <PackageLicenseFile>LICENSE</PackageLicenseFile>
    <PackageRequireLicenseAcceptance>False</PackageRequireLicenseAcceptance>
    <PackageReadmeFile>Readme.md</PackageReadmeFile>
    <PackageProjectUrl>https://apiverve.com/marketplace/api/randomfacts</PackageProjectUrl>
    <SignAssembly>False</SignAssembly>
    <Description>Random Facts is a simple tool for getting random facts. It returns a random fact.</Description>
    <RepositoryUrl>https://github.com/apiverve/randomfacts-NET</RepositoryUrl>
    <RepositoryType>git</RepositoryType>
    <PackageTags>random facts; random fact; random facts generator; random fact generator; random facts api; random fact api; random facts tool; random fact tool; random facts software; random fact software; random facts service; random fact service; random facts library; random fact library; random facts sdk; random fact sdk; random facts code; random fact code; random facts script; random fact script; random facts program; random fact program; random facts application; random fact application; random facts app; random fact app; random facts utility; random fact utility; random facts helper; random fact helper; random facts helper tool; random fact helper tool; random facts helper software; random fact helper software; random facts helper service; random fact helper service; random facts helper library; random fact helper library; random facts helper sdk; random fact helper sdk; random facts helper code; random fact helper code; random facts helper script; random fact helper script; random facts helper program; random fact helper program; random facts helper application; random fact helper application; random facts helper app; random fact helper app; random facts helper tool; random fact helper tool; random facts helper utility; random fact helper utility; random facts helper</PackageTags>
    <Copyright>©2024 APIVerve. All Rights Reserved</Copyright>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Newtonsoft.Json" Version="13.0.3" />
  </ItemGroup>

  <ItemGroup>
    <None Update="LICENSE">
      <Pack>True</Pack>
      <PackagePath>\</PackagePath>
    </None>
    <None Update="Readme.md">
      <Pack>True</Pack>
      <PackagePath>\</PackagePath>
    </None>
  </ItemGroup>

</Project>
